# ═══════════════════════════════════════════
# LokFee! — Environment Variables Template
# Copy to .env.local and fill in real values
# ═══════════════════════════════════════════

# ── Database (Turso) ──
DATABASE_URL=libsql://your-db-name-your-org.aws-us-east-1.turso.io
TURSO_AUTH_TOKEN=your-turso-auth-token

# ── NextAuth ──
AUTH_SECRET=your-32-char-secret-here
AUTH_URL=https://app.lokfeel.com
NEXTAUTH_URL=https://app.lokfeel.com

# ── App URLs ──
NEXT_PUBLIC_APP_URL=https://app.lokfeel.com
NEXT_PUBLIC_APP_NAME=LokFee!

# ── Google OAuth (REQUIRED for Google Login) ──
# NOTE: Already configured in Vercel Production via Firebase project.
# Only needed for LOCAL development. Production uses Vercel env vars.
# Firebase project Google OAuth Client ID: 185541962106-g136olrtj2mctj1m4c4m569funbuer3f
# Authorized redirect URI: https://app.lokfeel.com/api/auth/callback/google
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ── X/Twitter OAuth 2.0 (REQUIRED for X Login) ──
# NOTE: Already configured in Vercel Production.
# Only needed for LOCAL development. Production uses Vercel env vars.
# Twitter Client ID: Tk9UWGxxU2k3MDNwOHVDZjBTMks6MTpjaQ
# Callback URI: https://app.lokfeel.com/api/auth/twitter/callback
TWITTER_CLIENT_ID=
TWITTER_CLIENT_SECRET=

# ── Google Tag Manager (Optional) ──
# Get from: https://tagmanager.google.com → Container ID (GTM-XXXXXXX)
NEXT_PUBLIC_GTM_ID=

# ── Email (SMTP) ──
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_USER=resend
SMTP_PASSWORD=your-resend-api-key
SMTP_FROM=hello@lokfeel.com

# ── Cron Jobs ──
CRON_SECRET=your-cron-secret-here
