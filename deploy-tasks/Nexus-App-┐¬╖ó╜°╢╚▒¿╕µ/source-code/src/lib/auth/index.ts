// Barrel export for auth module
export { handlers, auth, signIn, signOut, hashPassword, verifyPassword, requireAuth, requireAdminAuth, getCurrentUser, getSession, isAdmin, redirectIfNotAuthenticated, redirectIfAuthenticated, getUserByEmail, getUserById, updateUserProfile } from './auth';
